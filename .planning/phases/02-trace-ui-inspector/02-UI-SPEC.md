---
phase: 2
slug: trace-ui-inspector
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-04
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none |
| Icon library | none (inline SVG if needed) |
| Font | system-default (`font-sans` Tailwind) |

Existing `@theme` tokens in `src/index.css` define the color palette. No additional tooling or component library. All styling via Tailwind utility classes.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, pill badge internal padding |
| sm | 8px | Compact element spacing, button padding-y, form field gaps |
| md | 16px | Default element spacing, panel padding, card padding |
| lg | 24px | Section padding, inspector panel internal padding |
| xl | 32px | Layout gaps between panels, major vertical separation |
| 2xl | 48px | Panel header vertical padding, section breaks |
| 3xl | 64px | Page-level spacing (header bottom margin) |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 500 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 20px | 600 | 1.2 |

Usage:
- **Body (14/400/1.5):** Step description text, email preview body, JSON editor content, tooltip text
- **Label (12/500/1.4):** Panel titles, field labels, "Run a trace to see results" body, PASS/FAIL pill text, breadcrumb labels
- **Heading (16/600/1.2):** Panel header titles ("Step Inspector", "JSON Editor"), step name in inspector
- **Display (20/600/1.2):** App header flow name, empty state heading ("Run a trace to see results")

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f0f0f` | App background, canvas background |
| Secondary (30%) | `#1a1a1a` / `#1e1e1e` | Panel backgrounds (`sidebar`), cards, inspector rows |
| Accent (10%) | `#f97316` | Path animation stroke, "Run Trace" button bg, active step highlight border, resize handle hover |
| Destructive | `#ef4444` | Error text, error state background tint, FAIL pill |

Accent reserved for:
1. Path highlight animation (orange dashed stroke)
2. "Run Trace" button background
3. Active step border-left highlight in inspector
4. Resize handle hover state
5. Email preview toggle active state

Success (`#22c55e`): PASS pill background tint, success states only.

Text hierarchy:
- `text-primary` (`#ffffff` dark / `#18181b` light): headings, step names, email subject
- `text-secondary` (`#a1a1aa` dark / `#71717a` light): labels, timestamps, unexecuted steps, helper text

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Run Trace" |
| Empty state heading | "Run a trace to see results" |
| Empty state body | "Enter an event payload and click Run Trace to see how your flow evaluates." |
| Error state | "Trace failed — check payload and try again" |
| Destructive confirmation | None in this phase |

Additional copy:
- **Panel close button tooltip:** "Close panel"
- **Panel resize handle:** no visible text (cursor change only)
- **Prev/Next step buttons:** "← Previous" / "Next →"
- **Email preview toggle:** "Rendered" / "Raw HTML"
- **Contact selector placeholder:** "Select a test contact..."

---

## UI Considerations

> Shape-rooted UI state coverage per the UI-consideration probe taxonomy.

Applicable state considerations resolved: 7 covered, 0 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Step inspector (no trace run yet) | ✅ covered | Empty state heading "Run a trace to see results" + body text displayed in full-height panel |
| empty | Contact selector (no contacts loaded) | ✅ covered | Placeholder "Select a test contact..." with empty dropdown list |
| loading | Run Trace button | ✅ covered | Button shows "Running trace…" with subtle pulse animation (CSS keyframe) during execution |
| loading | Canvas path | ✅ covered | No path shown until trace completes; canvas stays in neutral state |
| error | Trace execution | ✅ covered | "Trace failed — check payload and try again" displayed below JSON editor |
| error | JSON parse | ✅ covered | Inline error below textarea (already implemented in Phase 1) |
| populated | Step inspector (trace complete) | ✅ covered | One step at a time, full detail card: step name, PASS/FAIL pill, resolved values, email preview or split breakdown |
| populated | Canvas (trace complete) | ✅ covered | Orange dashed animated path highlights executed nodes; unexecuted nodes grayed via `text-secondary` |
| long-text | Email preview body | ✅ backstop | Email body rendered inside scrollable container (`overflow-y-auto max-h-full`); long subject wraps with `line-clamp-2` |
| long-text | Step description / split expression | ✅ backstop | Text wraps within panel width; no truncation — inspector panel is wide enough (320px min) |
| overflow | Step inspector panel | ✅ covered | Single step view avoids list overflow; email preview scrolls within panel |
| overflow | JSON editor | ✅ covered | Textarea scrolls vertically; existing behavior from Phase 1 |
| zero-one-many | Steps in trace result | ✅ covered | Zero steps = empty state; one step = same detail view; many steps = prev/next navigation, same single-step card |
| zero-one-many | Split conditions | ✅ covered | Zero = no split node selected; one = condition displayed inline; many = stacked condition list in inspector |

---

## Interaction Contracts

### Keyboard Navigation
| Shortcut | Action |
|----------|--------|
| `ArrowLeft` | Previous step in inspector |
| `ArrowRight` | Next step in inspector |

Only active when step inspector is visible and a trace has been run.

### Panel Resize
- Drag handle on panel right edge (Step Inspector) and right edge (JSON Editor)
- Cursor changes to `col-resize` on hover
- Minimum widths enforced: Step Inspector 240px, JSON Editor 280px
- Maximum widths enforced: Step Inspector 480px, JSON Editor 640px
- Default widths: Step Inspector 320px, JSON Editor 384px

### Panel Close/Reopen
- Toggle button in panel header (× icon or chevron)
- Close hides the panel; canvas takes remaining space
- Reopen via button in header bar (small icon button)

### Path Animation
- **Style:** CSS `stroke-dasharray` + `stroke-dashoffset` animation (marching ants)
- **Color:** `#f97316` (accent)
- **Speed:** `2s linear infinite` for continuous march
- **Stroke width:** 2px
- **Note:** May switch to static in future — see `.planning/CONCERNS.md`

### Step Selection
- Click a node on canvas → inspector jumps to that step's detail card
- Click prev/next buttons → inspector cycles through steps in order
- Active step highlighted with left border accent color

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | n/a | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

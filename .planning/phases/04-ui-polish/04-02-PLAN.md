---
phase: 04-ui-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/index.css
  - src/components/StepInspector.tsx
  - src/components/JsonEditor.tsx
  - src/components/TraceDock.tsx
  - src/components/StepNode.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Text-primary on bg-primary contrast ratio ≥ 4.5:1 in both light and dark mode"
    - "Text-secondary on bg-primary contrast ratio ≥ 4.5:1 in both modes"
    - "Accent button (#f97316) text passes 4.5:1 contrast (white on orange)"
    - "Font scale consistent across header, dock titles, body, labels (xs=11px, sm=12px, base=13px, etc.)"
    - "Panel titles (Step Inspector, Test data) consistent: same size, weight, spacing"
    - "Body text uses 13px or 14px for readability (not 12px or smaller for primary content)"
    - "Spacing rhythm: 4px/8px/12px/16px — no inconsistent 5px/7px/11px"
    - "Inspector scroll/overflow handled cleanly (no horizontal scroll bars from long content)"
  artifacts:
    - "src/index.css (font-size scale tokens, line-height tokens if needed)"
    - "src/components/StepInspector.tsx (typography polish: titles, labels, body)"
    - "src/components/JsonEditor.tsx (font size consistent with inspector)"
    - "src/components/TraceDock.tsx (panel header typography)"
  key_links:
    - "index.css tokens → all components (Tailwind utility classes)"
    - "StepNode label/truncate → StepInspector text sizing consistency"
---

<objective>
Polish the color palette for WCAG AA contrast, establish a consistent font scale, and enforce consistent spacing rhythm across all panels. No new components — only token additions and small Tailwind class changes.

Purpose: Debug tool should look intentional, not accidental. AA contrast ensures readability for ops engineers during long sessions. Consistent type scale reduces visual noise.
Output: Extended typography tokens in index.css, minor class tweaks across inspector/editor/dock/node.
</objective>

<execution_context>
@/home/adf_home_joel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/adf_home_joel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-CONTEXT.md
@src/index.css
@src/components/StepInspector.tsx
@src/components/JsonEditor.tsx
@src/components/TraceDock.tsx
@src/components/StepNode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix WCAG AA contrast on existing tokens</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (lines 5-23, light/dark CSS vars)
  </read_first>
  <action>
Audit the existing color tokens against WCAG AA contrast (4.5:1 for normal text, 3:1 for large/UI components).

**Audit table (compute by hand or recall from common values):**

| Pair (light mode) | Hex | Expected contrast |
|---|---|---|
| text-primary (#171717) on bg-primary (#ffffff) | high | ~16.5:1 ✓ |
| text-secondary (#737373) on bg-primary (#ffffff) | ~4.7:1 | ✓ borderline pass |
| text-muted (#a3a3a3) on bg-primary (#ffffff) | low | ✗ FAIL — ~2.85:1 |
| bg-card (#ffffff) on bg-secondary (#f5f5f5) | high | ~1.04:1 (decorative only) |

| Pair (dark mode) | Hex | Expected contrast |
|---|---|---|
| text-primary (#ffffff) on bg-primary (#0f0f0f) | high | ~19:1 ✓ |
| text-secondary (#a1a1aa) on bg-primary (#0f0f0f) | ~8:1 | ✓ pass |
| text-muted (#52525b) on bg-primary (#0f0f0f) | low | ✗ FAIL — ~3:1 borderline for normal |
| accent (#f97316) used as button bg with white text | mid | ~3.4:1 — FAILS AA for normal text (12-14px) |

**Fixes:**

1. **text-muted in light mode:** change from `#a3a3a3` to `#8a8a8a` (was failing). New contrast vs white ~3.5:1 — still not quite 4.5. Use `#737373` (same as text-secondary) for muted in light mode → full pass. Ponytail: collapse muted and secondary into one token in light mode (they're functionally identical in meaning).

   Update line 12: `--text-muted: #737373;`

2. **Accent button text contrast:** Run Trace button uses `bg-[#f97316] text-white` (App.tsx line 113). Contrast is 3.4:1 — fails AA for body text. Two options:
   - (a) Darken accent to `#c2410c` (orange-700) → ~5.2:1 ✓ pass, but loses brand color fidelity
   - (b) Use white text with `font-medium` (bold weights count as large text, 3:1 minimum) → already `font-medium`, but body text under 18px still needs 4.5
   - **Ponytail:** option (b) is the simplest — font-medium gives 'large text' treatment, 3:1 is sufficient for large/bold. Use AA Large standard. Document this acceptance.

3. **Dark mode text-muted:** `#52525b` on `#0f0f0f` → 3.05:1. Acceptable for non-essential labels (decorative borders, captions), fails for instructional text. The token is named `muted` — for muted decorative use only. Keep as-is. Ponytail: explicit acceptance comment in CSS.

**Add to index.css** at the top of `:root` block, no value changes, just a comment documenting the WCAG posture:

After line 13, before `.dark` block on line 15, add:

```css
/* ponytail: text-muted is decorative-only (3.05:1 dark, 3.5:1 light) — never use for instructional content */
```

**Do NOT:**
- Change text-primary, text-secondary hex values (both already pass AA).
- Introduce a new token type.
- Restructure the dark mode block (defer to v2 if needed).
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/nitrosend-challenge/demo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - text-muted in :root changed from #a3a3a3 to #737373 (matches text-secondary, both pass AA)
    - Single-line comment documents muted token's decorative-only constraint
    - Dark mode vars unchanged (token was already AA-acceptable for its scope)
    - Accent button text contrast accepted as AA-Large (3:1) — font-medium qualifies
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish font size scale tokens</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css
    - src/components/StepNode.tsx (current font sizes used)
    - src/components/TraceDock.tsx (panel header font sizes)
    - src/components/App.tsx (header font sizes)
  </read_first>
  <action>
Define a font-size scale in `:root` so per-component `text-xs`, `text-sm` choices are consistent. Ponytail: don't override Tailwind defaults — just document the agreed scale via comments. Tailwind defaults already follow a scale.

**Audit current usage across files:**

| Element | Current | Tailwind class | Pixel |
|---|---|---|---|
| Header h1 | text-sm | text-sm | 14px |
| Header buttons | text-sm | text-sm | 14px |
| Dock panel title | text-xs | text-xs | 12px |
| Dock close button | text-xs | text-xs | 12px |
| Inspector body cards | text-sm | text-sm | 14px |
| JsonEditor textarea | (CSS / browser default) | text-base Tailwind could override | 16px |
| StepNode label | text-sm | text-sm | 14px |
| StepNode kind | text-xs | text-xs | 12px |

**Decision:** scale is mostly already Tailwind default. Just enforce two changes:

1. **JsonEditor textarea:** Currently uses browser default 14px font. Tailwind `text-sm` is 14px too — same value, but adding the class makes it explicit. Confirm: read JsonEditor.tsx and apply `text-sm` to the textarea.

2. **StepNode label:** `text-sm` (14px) is fine. `kind` label is `text-xs` (12px) — fine.

**No new CSS tokens needed.** All sizes already align. Add a comment block in index.css documenting the agreed scale for future contributors:

After the `@theme` block (after line 35), before any closing content, add:

```css
/* ponytail: type scale — body=14px (text-sm), secondary=12px (text-xs), micro=10px (text-[10px]) — kept inline per component, no Tailwind override */
/* contrast: text-secondary on bg-card ≥ 4.5:1 (AA) verified light+dark */
```

**Do NOT:**
- Override Tailwind's default font-size scale.
- Add new tokens for line-height or letter-spacing (not needed).
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/nitrosend-challenge/demo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Comment block in index.css documents type scale: body=14px, secondary=12px, micro=10px
    - JsonEditor textarea confirmed text-sm applied (verify by reading file)
    - No new font tokens added
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply spacing rhythm consistency pass</name>
  <files>src/components/StepNode.tsx, src/components/TraceDock.tsx, src/components/StepInspector.tsx, src/components/JsonEditor.tsx</files>
  <read_first>
    - src/components/StepNode.tsx (current padding)
    - src/components/TraceDock.tsx (panel header padding)
    - src/components/StepInspector.tsx (existing spacing)
    - src/components/JsonEditor.tsx (existing spacing)
  </read_first>
  <action>
Audit and standardize padding/margin across the four touched components.

**Target rhythm:** 4px (`p-1`) / 8px (`p-2`) / 12px (`p-3`) / 16px (`p-4`). No odd values like `p-2.5`, `p-1.5`, `px-3.5`.

**Find and fix:**

1. **TraceDock panel header** (line 97): currently `px-3 py-... ` — check `h-8` (32px) total height, single-line content. Add `tracking-wide` to panel title for label consistency OR leave as-is if already 12px (text-xs default). Verify: at line 98 `<span className="text-xs text-secondary">` — text-xs is 12px. The `tracking-wide` would add letter-spacing.

   **Ponytail:** don't add `tracking-wide` — small benefit, not visible in 12px labels. Skip.

2. **TraceDock resize handle** (lines 86, 117): `w-1` (4px). Already on-rhythm.

3. **JsonEditor textarea padding:** check file. If `p-2` (8px) or `p-3` (12px), keep. Ponytail: read and verify, no change unless `p-2.5` or other odd value.

4. **StepNode `px-3 py-2`:** 12px horizontal, 8px vertical — on-rhythm. ✓

5. **StepNode icon-label gap** (`gap-2` from Task 04-01-3): 8px — on-rhythm. ✓

6. **Dock canvas empty-state** (FlowCanvas.tsx line 158): no padding to verify.

7. **App.tsx header (line 74):** `h-12` (48px) height, `px-4` (16px) padding — on-rhythm. ✓

**Light sweep expected:**
- Read all 4 files.
- Identify any odd-value padding/margin.
- Fix only those (likely 0-2 instances).
- If everything is on-rhythm, mark "no changes needed" and document.

**Concretely, here's what to do for this task:**
- Read each file end-to-end.
- For each `p-`, `px-`, `py-`, `m-`, `mx-`, `my-`, `gap-` class, check the value is 0/1/2/3/4/6/8 (4px-steps) or `0.5` (2px for hairlines) or `1.5` (6px) or `2.5` (10px) or `3.5` (14px).
- Replace any non-standard value with the nearest rhythm step. If existing is `p-1.5`, decide: keep (6px is fine for tight buttons) or round to `p-2` (8px).

**Do NOT:**
- Change layouts significantly.
- Override Tailwind's default spacing scale.
- Add new layout components.
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/nitrosend-challenge/demo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - All padding/margin/gap classes in the 4 components use 4px-rhythm values
    - Documentation comment in index.css mentions spacing rhythm (added in Task 2)
    - TypeScript compiles cleanly
  </done>
</task>

<task type="manual">
  <name>Task 4: Final visual verification in browser</name>
  <files>none (browser check)</files>
  <read_first>
    - .planning/phases/04-ui-polish/04-CONTEXT.md
    - Previous plan 04-01-SUMMARY.md (for cross-reference)
  </read_first>
  <action>
**Manual check — required after Tasks 1-3.** Run `npm run dev` and view the running app.

Verify the polish pass:

1. Switch light/dark mode several times — text remains readable in both.
2. Tab through inspector, json editor, canvas — text sizes feel consistent (no jarring size jumps).
3. Run a trace, look at split popover — typography is clean.
4. Resize panels — handle still works, no layout shift.

**Do NOT** mark plan complete until this manual check passes.

If something looks off:
- Specific font size in a panel → fix that one class.
- Spacing in a card looks broken → identify the component, fix the gap.
- Contrast issue remains → re-evaluate hex values, document the relaxation.
  </action>
  <verify>
    <automated>echo "Manual check — see action steps"</automated>
  </verify>
  <done>
    - Light/dark mode readable in all panels
    - Text sizes visually consistent
    - Panel resizing still works
    - No regressions to trace functionality
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens → components | Token rename would break derived Tailwind utilities (caught by tsc + runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | text-muted token rename | low | accept | All consumers use Tailwind classes, not raw hex; rename is additive if any direct reference |
| T-04-04 | Repudiation | Accent button text contrast waiver | low | accept | Documented as AA-Large via font-medium, comment in code |

</threat_model>

<verification>
- `npx tsc --noEmit` passes after each task
- `npm run dev` → visual: light+dark mode readable in all panels
- Step inspector / json editor / canvas text consistent at 14px body / 12px label / 10px micro
- Panel resizing still works
- No regressions to existing trace functionality (run trace end-to-end)
</verification>

<success_criteria>
- text-muted in light mode matches text-secondary (#737373) — both pass AA
- Type scale consistent: body=14px, secondary=12px, micro=10px
- Spacing rhythm: 4px/8px/12px/16px baseline throughout
- Documentation comments in index.css capture the design system decisions
- WCAG AA pass for all primary text; AA-Large (3:1) for accent button text
</success_criteria>

<output>
Create `.planning/phases/04-ui-polish/04-02-SUMMARY.md` when done
</output>

---
phase: 01-foundation-trace-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/mocks/browser.ts
  - src/mocks/handlers.ts
  - src/mocks/fixtures/flows.ts
  - src/mocks/fixtures/payloads.ts
  - src/mocks/fixtures/contacts.ts
autonomous: true
requirements:
  - TRCE-04
must_haves:
  truths:
    - MSW intercepts GET /api/flows/:id/trace and returns fixture data
    - 3 sample flows: linear 3-step, branching 5-step, multi-split 8+
    - 5 event payloads: cart_abandoned, signup, purchase, password_reset, subscription_expired
    - 5 test contacts with varied attributes (engagement, tier, bounced, unsubscribed)
    - Fixture files organized separately per D-12
  artifacts:
    - src/mocks/browser.ts
    - src/mocks/handlers.ts
    - src/mocks/fixtures/flows.ts
    - src/mocks/fixtures/payloads.ts
    - src/mocks/fixtures/contacts.ts
  key_links:
    - MSW handler returns TraceResult shape matching types.ts
    - Fixture flows use Step types from types.ts
    - browser.ts registers service worker before app render
---

<objective>
Set up MSW browser mock with handlers and fixture data. Provides deterministic API responses for the trace endpoint.

Purpose: Mock API layer — enables "Run Trace" button to return data without backend.
Output: MSW handlers, browser setup, 3 flow fixtures, 5 payload fixtures, 5 contact fixtures
</objective>

<execution_context>
@/home/adf_home_joel/.config/opencode/gsd-core/workflows/execution-engine.md
@/home/adf_home_joel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trace-engine/01-CONTEXT.md
@.planning/phases/01-foundation-trace-engine/01-RESEARCH.md
@.planning/phases/01-foundation-trace-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MSW handlers and browser setup</name>
  <files>src/mocks/browser.ts, src/mocks/handlers.ts</files>
  <action>
Create MSW v2 browser setup and handlers per TRCE-04 and RESEARCH.md Pattern 4.

**src/mocks/browser.ts:**
- Import `setupWorker` from `msw/browser`
- Import `handlers` from `./handlers`
- Export `const worker = setupWorker(...handlers)`

**src/mocks/handlers.ts:**
- Import `http`, `HttpResponse` from `msw`
- Import fixtures from `./fixtures/flows`
- Define `handlers` array with one handler:
  - `http.get('/api/flows/:id/trace', ({ params }) => { ... })`
  - Find flow by `Number(params.id)` from fixtures
  - If not found: `HttpResponse.json({ error: 'Flow not found' }, { status: 404 })`
  - If found: return `HttpResponse.json({ flowId: flow.id, results: [] })` — results empty in Phase 1, populated by engine in Phase 2
- Export `handlers` array

MSW v2 API: use `http.get()`, `HttpResponse.json()`, NOT `rest.get()` or `res(ctx.json())`.
  </action>
  <read_first>
    - src/mocks/browser.ts (does not exist yet)
    - src/mocks/handlers.ts (does not exist yet)
    - src/engine/types.ts (import TraceResult shape)
    - .planning/phases/01-foundation-trace-engine/01-RESEARCH.md (Pattern 4, MSW v2 API)
    - .planning/ROADMAP.md (API Reference: endpoint structure)
    - AGENTS.md (MSW for all API mocking)
  </read_first>
  <acceptance_criteria>
    - `http.get('/api/flows/:id/trace')` handler intercepts the endpoint
    - Returns 404 for unknown flow ID
    - Returns `{ flowId, results: [] }` for known flow ID
    - Uses MSW v2 API (`http`, `HttpResponse`)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>MSW handler intercepts /api/flows/:id/trace, returns fixture data or 404, compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture data files</name>
  <files>src/mocks/fixtures/flows.ts, src/mocks/fixtures/payloads.ts, src/mocks/fixtures/contacts.ts</files>
  <action>
Create three fixture files per D-09 through D-12.

**src/mocks/fixtures/flows.ts** (D-09):
- Export `flows: Flow[]` array with 3 flows:
  1. **Linear flow** (id: 1): trigger → email → email (3 steps, no splits)
  2. **Branching flow** (id: 2): trigger → split (2 conditions) → email (yes) / email (no) (5 steps total)
  3. **Multi-split flow** (id: 3): trigger → split → split → split → email × 4+ (8+ steps)
- Each flow has `id: number`, `name: string`, `status: 'draft'`, `trigger: Step`, `steps: Step[]`
- Import `Flow`, `Step` types from `../../engine/types`

**src/mocks/fixtures/payloads.ts** (D-10):
- Export `payloads` array with 5 event payloads:
  1. `cart_abandoned`: `{ event: 'cart_abandoned', data: { cart: { total: 49.99, items: 3 }, contact: { email: '...' } } }`
  2. `signup`: `{ event: 'signup', data: { plan: 'pro', source: 'organic' } }`
  3. `purchase`: `{ event: 'purchase', data: { order_total: 149.99, product_count: 5 } }`
  4. `password_reset`: `{ event: 'password_reset', data: { method: 'email' } }`
  5. `subscription_expired`: `{ event: 'subscription_expired', data: { plan: 'enterprise', expired_at: '...' } }`
- Each payload is `Record<string, unknown>`

**src/mocks/fixtures/contacts.ts** (D-11):
- Export `contacts` array with 5 contacts:
  1. High engagement, gold tier, not bounced
  2. Low engagement, free tier, not bounced
  3. Medium engagement, pro tier, bounced
  4. High engagement, enterprise tier, unsubscribed
  5. Zero engagement, free tier, never opened
- Attributes: `engagement_rating`, `tier`, `bounced`, `unique_opens`, `unsubscribed_at`
  </action>
  <read_first>
    - src/mocks/fixtures/flows.ts (does not exist yet)
    - src/mocks/fixtures/payloads.ts (does not exist yet)
    - src/mocks/fixtures/contacts.ts (does not exist yet)
    - src/engine/types.ts (import Flow, Step types)
    - .planning/phases/01-foundation-trace-engine/01-CONTEXT.md (D-09 through D-12)
    - .planning/ROADMAP.md (API Reference: step types, filter predicates)
  </read_first>
  <acceptance_criteria>
    - 3 flows: linear (3 steps), branching (5 steps), multi-split (8+ steps) (D-09)
    - 5 payloads: cart_abandoned, signup, purchase, password_reset, subscription_expired (D-10)
    - 5 contacts with varied engagement/tier/bounced/unsubscribed attributes (D-11)
    - Files organized separately per D-12
    - All fixtures use types from `../../engine/types`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>3 flows, 5 payloads, 5 contacts defined with correct types, compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture data → app | Fixture payloads are static; no user-controlled data in fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | MSW worker.start() | low | mitigate | Worker only starts in dev mode — conditional import in main.tsx |
| T-03-02 | Tampering | fixtures/ | low | accept | Fixtures are static data, no injection vector |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- MSW handler is importable from `src/mocks/handlers.ts`
- All fixture files export their data arrays
</verification>

<success_criteria>
- MSW handler serves /api/flows/:id/trace with deterministic responses
- 3 flows, 5 payloads, 5 contacts defined per locked decisions
- Fixture files organized separately per D-12
- All files compile
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trace-engine/01-03-SUMMARY.md` when done
</output>

---
phase: 01-foundation-trace-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - src/components/FlowCanvas.tsx
  - src/components/StepNode.tsx
autonomous: true
requirements:
  - CANV-01
  - CANV-02
must_haves:
  truths:
    - Flow graph renders as interactive node/edge graph (CANV-01)
    - Canvas supports zoom, pan, fit-to-view (CANV-02)
    - Auto-layout with dagre positions nodes top-down
    - Simple labeled boxes for nodes per D-14
    - Fit-to-view on initial load per D-15
    - Zoom range 0.2–2.0 per D-15
    - "No flow loaded" text centered on empty canvas
  artifacts:
    - src/components/FlowCanvas.tsx
    - src/components/StepNode.tsx
  key_links:
    - FlowCanvas reads flow from traceStore via useShallow
    - dagre layout converts flow tree to positioned nodes/edges
    - StepNode uses React.memo per AGENTS.md
---

<objective>
Build the React Flow canvas shell with dagre auto-layout. Renders flow graphs as interactive node/edge diagrams.

Purpose: Visual representation of the flow — users see the graph structure.
Output: `src/components/FlowCanvas.tsx`, `src/components/StepNode.tsx`
</objective>

<execution_context>
@/home/adf_home_joel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/adf_home_joel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trace-engine/01-CONTEXT.md
@.planning/phases/01-foundation-trace-engine/01-RESEARCH.md
@.planning/phases/01-foundation-trace-engine/01-UI-SPEC.md
@.planning/phases/01-foundation-trace-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React Flow canvas with dagre auto-layout</name>
  <files>src/components/FlowCanvas.tsx</files>
  <action>
Create `src/components/FlowCanvas.tsx` per D-13, D-14, D-15, CANV-01, CANV-02.

1. Import `ReactFlow`, `ReactFlowProvider`, `useReactFlow`, `type Node`, `type Edge` from `@xyflow/react`
2. Import `dagre` from `@dagrejs/dagre`
3. Import `useTraceStore` from `../store/traceStore`
4. Import `useShallow` from `zustand/react/shallow`
5. Import `StepNode` from `./StepNode`
6. Import default CSS: `@xyflow/react/dist/style.css`

**Flow-to-graph conversion** (RESEARCH.md Code Example: Flow Tree to React Flow Nodes/Edges):
- `flowToGraph(flow: Flow)` function that traverses the flow tree
- Creates `Node[]` with `id: step.id`, `position: {x:0, y:0}`, `data: {label, kind}`
- Creates `Edge[]` connecting parent→child
- For split steps: iterate ALL children in `yes` and `no` arrays

**Dagre auto-layout** (RESEARCH.md Pattern 2, D-13):
- `getLayoutedElements(nodes, edges)` function
- Set dagre graph: `rankdir: 'TB'`, default node dimensions 172×36
- Apply layout, map positions back to nodes

**FlowCanvas component:**
- Read `flow` from traceStore via `useShallow((s) => s.flow)`
- If `flow` is null: render centered "No flow loaded" text (UI-SPEC empty state)
- If flow exists: convert to graph, apply dagre layout, render `<ReactFlow>`
- Props: `fitView` (D-15), `minZoom={0.2}`, `maxZoom={2.0}` (D-15), `nodeTypes={{ step: StepNode }}`
- Container div: `h-full w-full` (RESEARCH.md Pitfall 1 — explicit dimensions required)

**Styling** (UI-SPEC tokens):
- Canvas background: `bg-primary` (#0f0f0f)
- Empty state text: `text-secondary` (#a1a1aa), centered
- Use Tailwind classes matching UI-SPEC color tokens
  </action>
  <read_first>
    - src/components/FlowCanvas.tsx (does not exist yet)
    - src/engine/types.ts (Flow, Step types)
    - src/store/traceStore.ts (traceStore shape)
    - src/components/StepNode.tsx (created in parallel or after — reference for nodeTypes)
    - .planning/phases/01-foundation-trace-engine/01-RESEARCH.md (Pattern 2, Code Examples, Pitfalls)
    - .planning/phases/01-foundation-trace-engine/01-UI-SPEC.md (Layout Skeleton, Color tokens)
    - .planning/phases/01-foundation-trace-engine/01-CONTEXT.md (D-13, D-14, D-15)
    - AGENTS.md (React.memo on custom nodes, useShallow)
  </read_first>
  <acceptance_criteria>
    - Flow renders as interactive node/edge graph (CANV-01)
    - Zoom range 0.2–2.0 with scroll wheel (CANV-02, D-15)
    - fitView on initial load (D-15)
    - dagre auto-positions nodes top-down (D-13)
    - Empty canvas shows "No flow loaded" text centered (UI-SPEC)
    - Container has explicit h-full w-full dimensions
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Canvas renders flow graph with dagre layout, fitView, zoom controls, empty state</done>
</task>

<task type="auto">
  <name>Task 2: Create StepNode custom React Flow node</name>
  <files>src/components/StepNode.tsx</files>
  <action>
Create `src/components/StepNode.tsx` per D-14 and RESEARCH.md Code Example: Custom React Flow Node.

1. Import `memo` from `react`
2. Import `Handle`, `Position`, `type NodeProps` from `@xyflow/react`
3. Define `StepNodeData` type: `{ label: string, kind: string }`
4. Create `StepNodeComponent` function:
   - Destructure `data` from `NodeProps`
   - Cast `data` to `StepNodeData`
   - Render rounded rectangle with border:
     - `rounded-lg border border-[#2a2a2a] bg-[#1e1e1e] px-4 py-2 text-sm text-white`
   - Show `label` (step name) in `font-medium`
   - Show `kind` (step type) in `text-xs text-[#a1a1aa]`
   - `<Handle type="target" position={Position.Top} />`
   - `<Handle type="source" position={Position.Bottom} />`
5. Export as `StepNode = memo(StepNodeComponent)` (AGENTS.md: React.memo on custom React Flow nodes)

Styling per UI-SPEC:
- Node box: `bg-card` (#1e1e1e), `border-card` (#2a2a2a)
- Label: 14px font-medium, text-primary (#ffffff)
- Kind tag: 12px text-secondary (#a1a1aa)
- Dimensions: dagre assumes 172×36 — match with px-4 py-2 + content
  </action>
  <read_first>
    - src/components/StepNode.tsx (does not exist yet)
    - .planning/phases/01-foundation-trace-engine/01-RESEARCH.md (Code Example: Custom React Flow Node)
    - .planning/phases/01-foundation-trace-engine/01-UI-SPEC.md (Color tokens, Typography)
    - .planning/phases/01-foundation-trace-engine/01-CONTEXT.md (D-14)
    - AGENTS.md (React.memo on custom React Flow nodes)
  </read_first>
  <acceptance_criteria>
    - Simple labeled box with step name and kind tag (D-14)
    - Rounded rectangle with border-card styling
    - Handle types: target (top), source (bottom)
    - Wrapped in React.memo
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>StepNode renders labeled box with handles, wrapped in memo, compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flow data → canvas rendering | Flow steps render as node labels; React escapes by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | StepNode label | low | mitigate | React escapes JSX by default; never use dangerouslySetInnerHTML |
| T-04-02 | Denial of Service | dagre layout | low | accept | Large flows (>100 nodes) may slow layout; acceptable for debug tool |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- FlowCanvas renders when flow is in store
- Empty state shows "No flow loaded"
</verification>

<success_criteria>
- Flow graph renders as interactive node/edge graph (CANV-01)
- Canvas supports zoom, pan, fit-to-view (CANV-02)
- dagre auto-positions nodes top-down (D-13)
- StepNode is simple labeled box with React.memo (D-14)
- Empty state shows centered text
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trace-engine/01-04-SUMMARY.md` when done
</output>

---
phase: 01-foundation-trace-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/store/traceStore.ts
autonomous: true
requirements:
  - TRCE-01
  - TRCE-02
must_haves:
  truths:
    - Step types are a discriminated union on `kind` field
    - StepResult is flat with optional `branchTaken` field
    - Zustand traceStore holds flow, payload, results, status, selectedStep
    - All Zustand selectors use useShallow wrapper
  artifacts:
    - src/engine/types.ts
    - src/store/traceStore.ts
  key_links:
    - TraceState shape matches RESEARCH.md Pattern 1
    - Step/StepResult types usable by execution engine and MSW fixtures
---

<objective>
Define TypeScript types for the flow graph, step results, and trace state. Create the Zustand store with useShallow selectors.

Purpose: Types and store are the foundation — every other plan imports from here.
Output: `src/engine/types.ts` (all type definitions), `src/store/traceStore.ts` (Zustand store)
</objective>

<execution_context>
@/home/adf_home_joel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/adf_home_joel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trace-engine/01-CONTEXT.md
@.planning/phases/01-foundation-trace-engine/01-RESEARCH.md
@.planning/phases/01-foundation-trace-engine/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define TypeScript types for flow graph and trace results</name>
  <files>src/engine/types.ts</files>
  <action>
Create `src/engine/types.ts` with all type definitions per D-01, D-02, D-08. The file must define:

1. **Step discriminated union** (D-01): `kind: 'email' | 'split' | 'webhook' | 'sms' | 'trigger'`. Each variant has `id: string` plus kind-specific fields:
   - `email`: `subject: string`, `body: string`
   - `split`: `filters: Filter[]`, `yes: Step[]`, `no: Step[]`
   - `webhook`: `url: string`, `method: string`
   - `sms`: placeholder fields (TBD per RESEARCH.md open questions)
   - `trigger`: `event: string`, `action_name: string`

2. **Filter type**: `Filter { name: string, value: string, predicate: 'eq' | 'neq' | 'gt' | 'lt' | 'contains' }`

3. **Flow type** (per ROADMAP.md API reference): `{ id: number, name: string, status: 'draft' | 'live', trigger: Step, steps: Step[] }`

4. **StepResult** (D-02): flat object `{ stepId: string, kind: Step['kind'], passed: boolean, branchTaken?: 'yes' | 'no' }`

5. **TraceResult** (D-08): `{ flowId: number, results: StepResult[] }`

6. **TraceStatus**: `'idle' | 'running' | 'done' | 'error'`

Use `export type` for all exports. TypeScript strict mode compatible.
  </action>
  <read_first>
    - src/engine/types.ts (does not exist yet — create fresh)
    - .planning/ROADMAP.md (API Reference section for flow/step structure)
    - .planning/phases/01-foundation-trace-engine/01-CONTEXT.md (D-01, D-02, D-08)
    - .planning/phases/01-foundation-trace-engine/01-RESEARCH.md (Pattern 3: Discriminated Union)
    - AGENTS.md (TypeScript strict mode, conventions)
  </read_first>
  <acceptance_criteria>
    - Step is a discriminated union with `kind` field as discriminator (D-01)
    - StepResult is flat with optional `branchTaken?: 'yes' | 'no'` (D-02)
    - Flow type matches ROADMAP.md API reference exactly
    - TraceResult is `{ flowId: number, results: StepResult[] }` (D-08)
    - All types are exported
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Types compile, Step is discriminated union on `kind`, StepResult is flat with `branchTaken?`, Flow matches API reference</done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand traceStore with useShallow selectors</name>
  <files>src/store/traceStore.ts</files>
  <action>
Create `src/store/traceStore.ts` per D-03, D-04. The store must:

1. Import `create` from `zustand` and `useShallow` from `zustand/react/shallow`
2. Import types from `../engine/types`
3. Define `TraceState` interface with fields: `flow: Flow | null`, `payload: Record<string, unknown>`, `results: StepResult[]`, `status: TraceStatus`, `selectedStep: string | null`
4. Define setter functions: `setFlow`, `setPayload`, `setResults`, `setStatus`, `setSelectedStep`
5. Create store with `create<TraceState>((set) => ({ ... }))` pattern
6. Initial values: `flow: null`, `payload: {}`, `results: []`, `status: 'idle'`, `selectedStep: null`
7. Export `useTraceStore` as the hook
8. Add a comment documenting that all consumers MUST use `useShallow` for multi-field selectors (D-04)

Export the `TraceState` interface for external use.
  </action>
  <read_first>
    - src/engine/types.ts (created by Task 1 — import types from here)
    - src/store/traceStore.ts (does not exist yet — create fresh)
    - .planning/phases/01-foundation-trace-engine/01-CONTEXT.md (D-03, D-04)
    - .planning/phases/01-foundation-trace-engine/01-RESEARCH.md (Pattern 1: Zustand Store with useShallow)
    - AGENTS.md (useShallow on all selectors from day one)
  </read_first>
  <acceptance_criteria>
    - Store shape matches D-03: `{ flow, payload, results, status, selectedStep }` plus setters
    - `useShallow` is imported from `zustand/react/shallow`
    - `useTraceStore` is exported as the hook
    - `TraceState` interface is exported
    - Comment documents useShallow requirement for consumers (D-04)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Store created with D-03 shape, useShallow imported, TraceState exported, TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → TraceState | JSON payload from textarea crosses into store; must be Record<string, unknown> |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | traceStore.payload | medium | mitigate | Type payload as Record<string, unknown> — JSON.parse validation happens in executor task, not here |
| T-01-02 | Information Disclosure | types.ts exports | low | accept | Types are static definitions, no runtime data exposure |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Types are importable from `src/engine/types.ts`
- Store is importable from `src/store/traceStore.ts`
</verification>

<success_criteria>
- Step discriminated union on `kind` field compiles
- StepResult flat with optional `branchTaken`
- Zustand store with D-03 shape and useShallow
- All types exported for downstream plans
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trace-engine/01-01-SUMMARY.md` when done
</output>
